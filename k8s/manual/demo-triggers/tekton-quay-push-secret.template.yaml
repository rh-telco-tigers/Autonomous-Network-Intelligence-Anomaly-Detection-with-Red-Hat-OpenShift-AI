# Template only. Do not commit real credentials into this file.
# Quay can download a Kubernetes secret in this exact format. Keep the name
# `autonomousnetworkintelligence-ocpani-pull-secret` so Tekton mounts it automatically.
apiVersion: v1
kind: Secret
metadata:
  name: autonomousnetworkintelligence-ocpani-pull-secret
  namespace: ani-tekton
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: REPLACE_WITH_BASE64_DOCKERCONFIGJSON
