apiVersion: batch/v1
kind: Job
metadata:
  name: ani-featurestore-bootstrap
  namespace: ani-demo-lab
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "41"
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: ani-featurestore-bootstrap
      containers:
        - name: bootstrap
          image: registry.redhat.io/openshift4/ose-cli@sha256:bc35a9fc663baf0d6493cc57e89e77a240a36c43cf38fb78d8e61d3b87cf5cc5
          command:
            - /bin/bash
            - -ceu
            - |
              namespace="${POD_NAMESPACE:-ani-demo-lab}"
              oc rollout status deploy/feast-ani-featurestore -n "${namespace}" --timeout=300s
              oc exec deploy/feast-ani-featurestore -n "${namespace}" -c registry -- bash -lc 'feast apply'
              oc label featurestore/ani-featurestore -n "${namespace}" opendatahub.io/dashboard=true --overwrite || true
              oc annotate featurestore/ani-featurestore -n "${namespace}" openshift.io/display-name='ANI Feature Store' --overwrite || true
              oc label route/feast-ani-featurestore-ui service/feast-ani-featurestore-ui deployment/feast-ani-featurestore -n "${namespace}" opendatahub.io/dashboard=true --overwrite || true
              oc annotate route/feast-ani-featurestore-ui -n "${namespace}" openshift.io/display-name='ANI Feature Store UI' --overwrite || true
              oc annotate service/feast-ani-featurestore-ui -n "${namespace}" openshift.io/display-name='ANI Feature Store UI' --overwrite || true
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
