apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ims-demo-operators
  namespace: openshift-gitops
spec:
  destination:
    namespace: openshift-gitops
    server: https://kubernetes.default.svc
  project: ims-demo
  source:
    path: deploy/gitops/operators
    repoURL: https://github.com/pandeybk/IMS-Anomaly-Detection-with-Red-Hat-OpenShift-AI.git
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
