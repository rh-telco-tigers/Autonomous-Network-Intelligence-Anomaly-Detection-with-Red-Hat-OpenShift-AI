# Model Artifacts

This directory stores predictive model artifacts produced by the AutoGluon-plus-baseline training flow in `ai/training/train_and_register.py`.

The live predictive serving artifact is exported in Triton model repository layout under `ai/models/serving/predictive/ims-predictive/`.
