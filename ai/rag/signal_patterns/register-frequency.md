# REGISTER Frequency Anomaly Pattern

Sustained `REGISTER` bursts with low inter-arrival mean and elevated retransmission counts usually indicate retry amplification or bot-like registration behavior. Correlate the traffic pattern with P-CSCF concurrency and HSS response time before classifying the incident as an application-layer overload.
