# RCA Knowledge Base

This directory contains seed runbooks for RCA retrieval and a bootstrap script that can load them into Milvus using a deterministic hash embedding.

## Files

- `runbooks/*.md`: demo RCA knowledge sources
- `bootstrap_knowledge.py`: loads the runbooks into a Milvus collection named `ims_runbooks`

