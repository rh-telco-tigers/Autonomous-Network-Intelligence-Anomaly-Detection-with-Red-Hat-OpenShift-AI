# sipp-runner

Runs SIPp scenarios against the IMS lab and turns the resulting traces into labeled feature windows.

It is the bridge between generated SIP traffic and reusable training data stored in object storage for the pipeline.
