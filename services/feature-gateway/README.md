# feature-gateway

Builds feature windows that represent IMS signaling behavior in a consistent schema.

It can drive live SIP traffic into the lab, try to read IMS telemetry, and fall back to deterministic scenario data so downstream scoring always receives usable features.
